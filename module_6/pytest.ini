[pytest]
pythonpath =
    src/db
    src/web/app
    src/web
    src
addopts = --strict-markers --cov=app.flask_app --cov=app.blueprints.dashboard --cov=load_data --cov=query_data --cov=module_2.clean --cov=module_2.run --cov=module_2.scrape --cov-report=term-missing --cov-fail-under=100
markers =
    web: page load and HTML structure tests
    buttons: button endpoints and busy-state behavior tests
    analysis: analysis labels and percentage formatting tests
    db: database schema, inserts, and selects tests
    integration: end-to-end flow tests
